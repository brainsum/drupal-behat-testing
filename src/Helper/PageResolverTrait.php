<?php

namespace Brainsum\DrupalBehatTesting\Helper;

trait PageResolverTrait {

  protected $pageNameMapping;

  // @todo: Get these from config.
  // /**
  //  * Reads the config.yml file and populates the $config property.
  //  */
  // public function loadConfig() {
  //   $this->config = (new \Symfony\Component\Yaml\Parser())->parse(file_get_contents(static::CONFIG_FILE));
  // }
  // path-mapping.yml

}
